# app/api/routers/__init__.py
# Marks the `routers` directory as a Python package.
# Individual APIRouter modules will be defined here in future phases.
# Example future modules:
#   - documents.py  (upload, list, delete documents)
#   - search.py     (semantic search endpoints)
#   - query.py      (RAG Q&A endpoints)
#   - health.py     (health-check and status endpoints)
# Each router will be imported and registered onto the main FastAPI app in app/main.py.
